# Export all public methods, that is, those without a preceeding dot
# in their names.
exportPattern("^[^\\.]")

importFrom("stats", "median", "pnorm")

useDynLib("HaarSeg", .registration = TRUE, .fixes = "C_")
